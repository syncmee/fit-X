# fiT-X configuration. Copy to .env and fill in your values.
# .env is gitignored — never commit real credentials.

# Flask
FLASK_ENV=development
SECRET_KEY=change-me-to-a-long-random-string

# Database (omit DATABASE_URL to use the local SQLite file at instance/userdata.db)
# Supabase example (pooled connection, works over IPv4):
# DATABASE_URL=postgresql://postgres.<project-ref>:<password>@aws-0-<region>.pooler.supabase.com:6543/postgres?sslmode=require

# Session cookie SameSite policy (Lax / Strict / None)
SESSION_COOKIE_SAMESITE=Lax

# fiT-X AI coach — get a key at https://aistudio.google.com/
# Leave GEMINI_API_KEY empty to use the built-in rule-based coach instead.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.5-flash-lite

# fiT-X AI coach fallback (Groq, https://console.groq.com/keys). Used when the
# Gemini call fails (overloaded model, quota, outage); leave empty to skip.
GROQ_API_KEY=
GROQ_MODEL=openai/gpt-oss-120b

# Sign in with Google (OAuth 2.0). Create an OAuth client at
# console.cloud.google.com/apis/credentials and add these redirect URIs:
#   https://<your-host>/auth/google/callback  and  http://127.0.0.1:5000/auth/google/callback
# Leave empty to hide/disable the Google button flow.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Workout reminder emails via SMTP. Brevo works without a domain: verify the
# sender email at app.brevo.com, generate an SMTP key, then:
#   SMTP_HOST=smtp-relay.brevo.com  SMTP_PORT=2525  SMTP_FROM=<verified sender>
# (port 2525 because Render's free tier blocks outbound SMTP on 25/465/587)
# Leave SMTP_USER empty to run /cron/send-reminders in dry-run mode.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_APP_PASSWORD=
SMTP_FROM=

# Shared secret for the scheduler -> /cron/send-reminders trigger.
# Must match the FITX_CRON_SECRET repo secret used by .github/workflows/reminders.yml
CRON_SECRET=
